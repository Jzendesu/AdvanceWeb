// Create an object:
const mcat = {type:"Puspin", age:"10 months", color:" white"};

// Display some data from the object:
document.getElementById("objects").innerHTML = "The cat is " + mcat.type + mcat.color;